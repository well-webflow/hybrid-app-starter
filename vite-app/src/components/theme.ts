import { createTheme } from "@mui/material";

const theme = createTheme({
  typography: {
    fontSize: 9,
    body1: {
      fontSize: "1rem",
      lineHeight: 1.5,
    },
  },
});

export { theme };
